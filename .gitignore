# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
.venv-*/

# Environment / secrets
.env
.env.*
*.env

# Build
build/
dist/

# Model weights & data
*.pt
*.pth
*.parquet
*.csv
demo_data.json

# Local benchmark / training reports (not source of truth)
pulse/benchmark.report*.json

# OS
.DS_Store

# gents: per-repo claude memory, and the local pointer to it.
# Agent-written and machine-specific — must never ride a commit.
/.claude/memory
