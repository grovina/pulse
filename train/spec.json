{
  "schemaVersion": "v1",
  "app": "pulse",
  "iter": 89,
  "hypothesis": "Iter 89 = maximal-faithfulness overhaul folding in ALL known radar improvements (user: 'gate is an arbitrary milestone, the goal is a BETTER MODEL'; 'go for it all'). Follows iter-88 (trainer-k7n4p) which fixed glucose (0.2733->0.1930 PASS, per-patient Ra head worked) but REGRESSED hr (0.1858->0.2061, now the SOLE gate blocker) — halving default-baseline-weight (0.30->0.15) let HR re-inflate. Core finding: the CardiovascularModule was a bare learned-MLP with NO setpoint term, so each patient's resting HR/HRV/SBP/DBP had to be inferred through 12h of integrated rate — the iter-36 investigation proved that fails (HR drifts ~+15 bpm). The TEACHER (full_body.py) models every vital as relaxation to a per-patient setpoint + learned drivers (dHR=-k_hr*(HR-HR0-circ-sleep)+cort_drive+act_gain, per-patient HR0/HRV0/SBP0/DBP0). This is the SAME gap the metabolic minimal-model closed for glucose across iters 81-88. FIXES (A+B+C+D): (A cardiovascular.py) give ALL 4 CVS vitals the teacher's ADDITIVE first-order setpoint form: rate = learned_driver - k*(norm_state - setpoint_emb). setpoint_net emits per-patient offsets for [hr,hrv,sbp,dbp] (tanh-bounded +-3.0z, zero-init final layer => setpoint=NORM_CENTER at cold start), per-vital bounded restoring rate k. NOT the iter-36 dead-end (that CENTERED the MLP input, shifting the coupling frame between train and zero-embedding calibration -> misfire); here the MLP still sees uncentered state and only an explicit restoring force is added. (B types.py+metabolic.py+model.py) replace the INSTANTANEOUS insulin action x_ins=Si*relu(insulin) with the teacher's DYNAMIC lagged remote-insulin state (Bergman dX=-p2*X+p3*max(I-Ib,0), tau~33min) as a new internal metabolic state appended at the end (index preserved like crh/glycogen); glucose clearance now uses the lagged X. (C dose_response.py) sharpen the dose-response peak estimator (post_window 180->60, softargmax_beta 0.10->3.0) so amplitude supervision measures the true PEAK the 0.7 mg/dL/g literature target is defined against, not a 180-min soft-mean. (D trainArgs) restore default-baseline-weight 0.15->0.30 — with per-patient CVS setpoints now housing athlete lows, the firm anchor only pins the DEFAULT patient at 70 and no longer fights per-patient calibration, so iter-88's re-inflation downside disappears. Keeps ALL iter-86/87/88 structure (minimal-model glucose setpoint, Gb_emb, Ra head, Sg band, meal-timing fix).",
  "expectedEffect": "PRIMARY (model quality, not just gate): hr resting level becomes per-patient-correct (hr_mape 0.2061 -> target <0.15) via the CVS setpoint; BP calibration also becomes per-patient (sbp/dbp gain their own setpoints); meal dynamics gain the physically-correct insulin-action lag; amplitude supervision becomes unbiased. SECONDARY: glucose_mape HOLDS <=0.20 (CVS is a pure coupling SINK — no CVS state feeds glucose/temp; the only shared conduit is the embedding, and zero-init additive heads REDUCE entanglement); temp HOLDS <0.02; overall_weighted_mape improves from 0.1062; verifier meal/coupling/circadian improve. WATCH: (R1) sbp/dbp must HOLD ~0.043/0.058 — they pass comfortably today and the CVS setpoint adds a restoring term to their (shared-trunk) dynamics; if either regresses, verifier_cat[coupling] is the iter-36 canary (was 0.876->0.541 when the coupling frame broke). (R2) the new insulin-action state B sits next to the glucose long-rollout that CRASHED iters 83-85 (exit 42, phase-2 epoch 51 NaN) — MANDATORY pre-dispatch check: 24h-rollout backward has ZERO non-finite grads; if unstable, stage B out to iter-90 and dispatch A+C+D. (R3) glucose (0.1930/0.20) and temp (0.0182/0.02) are thin-margin — verify both on the result. (R4) all new heads zero-init => cold start has no per-patient perturbation; per-patient authority is learned. (R5) glucose/hr skill_vs_persistence stays deeply negative BY CONSTRUCTION (ratio vs ~1% persistence floor on near-flat windows, documented since iter-77) — NOT a gate metric, do not chase.",
  "sourceRef": "HEAD",
  "params": {
    "trainArgs": [
      "--phase1-epochs=50",
      "--phase2-epochs=10",
      "--phase2-lr=0.0003",
      "--verifier-loss-weight=0.03",
      "--cohort-statistic-weight=0.15",
      "--cohort-sample-patients=6",
      "--cohort-statistic-adaptive",
      "--trajectory-band=0.08",
      "--trajectory-band-default=0.05",
      "--landmark-weight=0.20",
      "--landmark-pre-window=15",
      "--landmark-post-window=120",
      "--landmark-min-carbs=5.0",
      "--dose-response-weight=0.40",
      "--dose-response-sample-patients=4",
      "--dose-response-markers=glucose:peak:0.7:0.25;insulin:peak:0.4:0.3;glp1:rank:0.3",
      "--gut-dose-sweep-weight=0.10",
      "--gut-dose-sweep-sample-patients=4",
      "--gut-dose-sweep-auc-weight=5.0",
      "--insulin-sweep-weight=0.30",
      "--insulin-sweep-sample-patients=4",
      "--insulin-sweep-auc-weight=5.0",
      "--insulin-sweep-ranking-weight=1.0",
      "--fasting-stability-weight=0.10",
      "--fasting-stability-window=240",
      "--postprandial-recovery-weight=0.10",
      "--default-baseline-weight=0.30",
      "--default-baseline-markers=hr:lactate",
      "--carb-mass-balance-weight=0.05",
      "--cold-distill-weight=0.3",
      "--cold-distill-markers=insulin:lactate:hepatic_output:glucagon:ffa:ghrelin:leptin:acth:cortisol:bhb:liver_glycogen:muscle_glycogen",
      "--cold-distill-protocols-per-epoch=4",
      "--cold-distill-pool=both",
      "--cold-distill-mode=anchored",
      "--cold-distill-anchor-window=60",
      "--cold-distill-anchor-samples=8",
      "--physiology-rules-weight=0.05",
      "--physiology-rules-sample-patients=4",
      "--physiology-rules-adaptive",
      "--n-default-patients=8",
      "--n-patients=20",
      "--n-epochs=80",
      "--hidden-dim=48",
      "--lr=0.003",
      "--n-days=14",
      "--windows-per-patient=3",
      "--meal-window-bias=0.55",
      "--gut-loss-weight=0.0"
    ]
  }
}
